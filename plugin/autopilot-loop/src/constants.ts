// Defines stable autopilot-loop hook constants.
export const HOOK_NAME = "autopilot-loop"

// Defines default state storage path relative to project root.
export const DEFAULT_STATE_FILE = ".opencode/autopilot-loop.state.json"

// Defines default maximum continuation iterations.
export const DEFAULT_MAX_ITERATIONS = 100

// Defines default completion promise token.
export const DEFAULT_COMPLETION_PROMISE = "DONE"
