# v1.6 Reason-Code Export Plan

## Goal

Add first-class baseline export support for `/doctor reason-codes` so drift comparisons can be generated from deterministic snapshots.

## Status Legend

- pending
- in progress
- done
- blocked

## Validation Gates

- `make validate`
- `make selftest`
- `make install-test`
- `pre-commit run --all-files`

## Worklog

| UTC | Task | Status | Notes |
|---|---|---|---|
| 2026-03-01T09:20:00Z | Plan scaffold | done | Opened v1.6 tracking for reason-code baseline export. |
| 2026-03-01T09:30:00Z | E1 reason-code baseline write support | done | Added `/doctor reason-codes --write <path>` with baseline-compatible payload export and selftest coverage. |

## Epics

### E1 Reason-code baseline write support

- [x] Add `/doctor reason-codes --write <path>` export support
- [x] Keep export format compatible with `--diff <baseline.json>` input
- [x] Add docs + selftest coverage
