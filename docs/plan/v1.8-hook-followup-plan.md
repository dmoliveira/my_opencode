# v1.8 Hook Follow-up Plan

Lower-priority follow-up items from the AI hook and injection audit after the validation/guidance hardening batch landed.

## Remaining Items

1. Rules docs/runtime alignment
   - Decide whether runtime should also scan user-scope rule paths, or trim docs to match the current runtime loader surface.
   - Primary runtime file: `plugin/gateway-core/src/hooks/rules-injector/index.ts`

2. Gateway sidecar config operator docs
   - Add one short operator-facing note for `.opencode/gateway-core.config.json` in `README.md`, `docs/quickstart.md`, or `docs/operator-playbook.md`.

3. Broader first-turn reminder triggering
   - Expand `agent-user-reminder` beyond the current complex-task regex so common prompts like "implement", "migrate", and "ship" trigger guidance more reliably.

4. Mistake-ledger feedback loop
   - Delivered via `/gateway status --json`, which now reports `mistake_ledger` summary data for `.opencode/mistake-ledger.jsonl` so operators can see whether validation deferrals are accumulating.

## Delivered In This Follow-up

- reset validation evidence on `session.compacted`
- reload rule files on each injection pass so long-lived sessions pick up rule edits without restart
