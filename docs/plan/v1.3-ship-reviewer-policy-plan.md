# v1.3 Ship Reviewer Policy Plan

## Goal

Add deterministic reviewer policy controls to `/ship create-pr` with CODEOWNERS fallback plus allow/deny filtering.

## Status Legend

- pending
- in progress
- done
- blocked

## Validation Gates

- `make validate`
- `make selftest`
- `make install-test`
- `pre-commit run --all-files`

## Worklog

| UTC | Task | Status | Notes |
|---|---|---|---|
| 2026-03-01T09:00:00Z | Plan scaffold | done | Opened v1.3 tracking for ship reviewer policy controls. |
| 2026-03-01T09:10:00Z | E1 reviewer policy filters | done | Added allow/deny reviewer policy filtering and payload diagnostics for `/ship create-pr` with selftest coverage. |

## Epics

### E1 Reviewer policy filters

- [x] Add allow/deny reviewer policy filtering for `/ship create-pr`
- [x] Apply filters consistently to explicit reviewers and CODEOWNERS fallback reviewers
- [x] Emit structured policy diagnostics (`allowed`, `denied`, `filtered_out`) in payload
- [x] Add docs + selftest coverage
