# v1.4 Hotfix Followup Link Plan

## Goal

Add a follow-up issue link helper to `/hotfix postmortem` so operators can move directly from template output to tracked remediation.

## Status Legend

- pending
- in progress
- done
- blocked

## Validation Gates

- `make validate`
- `make selftest`
- `make install-test`
- `pre-commit run --all-files`

## Worklog

| UTC | Task | Status | Notes |
|---|---|---|---|
| 2026-03-01T09:20:00Z | Plan scaffold | done | Opened v1.4 tracking for hotfix follow-up link helper. |
| 2026-03-01T09:28:00Z | E1 postmortem follow-up link helper | done | Added `/hotfix postmortem --link-followup` URL resolution with deterministic status codes and selftest coverage. |

## Epics

### E1 Postmortem follow-up link helper

- [x] Add `/hotfix postmortem --link-followup` URL resolution
- [x] Resolve follow-up issue URL via `gh issue view` when issue id is present
- [x] Keep payload deterministic when issue URL cannot be resolved
- [x] Add docs + selftest coverage
