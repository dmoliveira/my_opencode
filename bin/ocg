#!/usr/bin/env bash
set -euo pipefail

cmd="${1:-status}"
case "$cmd" in
  on|enable)
    exec opencode-mcp enable gh_grep
    ;;
  off|disable)
    exec opencode-mcp disable gh_grep
    ;;
  status)
    exec opencode-mcp status
    ;;
  *)
    exec opencode-mcp "$@"
    ;;
esac
