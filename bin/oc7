#!/usr/bin/env bash
set -euo pipefail

cmd="${1:-status}"
case "$cmd" in
  on|enable)
    exec opencode-mcp enable context7
    ;;
  off|disable)
    exec opencode-mcp disable context7
    ;;
  status)
    exec opencode-mcp status
    ;;
  *)
    exec opencode-mcp "$@"
    ;;
esac
